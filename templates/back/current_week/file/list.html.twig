{% extends 'back/admin.html.twig' %}

{% block title %}Liste des téléchargements - {% block title_base %}{{ parent() }}{% endblock %}{% endblock %}
{% block admin_page_title %}Telechargements{% endblock %}

{% block admin_toolbar %}
    <div class="admin-toolbar">
        <input type="search" class="form-control js-admin-filter-input" placeholder="Filtrer les fichiers...">
        <span class="text-muted small">Historique des exports PDF</span>
    </div>
{% endblock %}

{% block content %}
			<table class="table js-admin-table" data-toggle="table">
				<thead class="bg-blue-dark-light">
					<tr>
                        <th class="color-yellow" scope="col">Nom</th>
						<th class="color-yellow" scope="col">Date de téléchargement</th>
						<th class="color-yellow" scope="col">Action</th>
					</tr>
				</thead>
				<tbody class="bg-blue-dark table-borderless">
					{% for file in file %}
						<tr>
							<td  class="color-white text-bold">{{ file.name }}</td>
							<td  class="color-white text-bold">{{ file.createdat|date("d-m-Y", "Europe/Paris")}} à {{file.createdat|date("H:i", "Europe/Paris")}}</td>
                            <td>
								<a class="admin-icon-btn" href="{{ asset("pdf/" ~ file.name) }}" target="new" title="Voir"><i class="fas fa-eye"></i></a>
								<a class="admin-icon-btn" href="{{ path('delete_download', {"id": file.id}) }}" title="Supprimer" onclick="return confirm('Attention vous vous apprettez à supprimer un téléchargement')"><i class="fas fa-trash"></i></a>
                            </td>
						</tr>
					{% endfor %}
				</tbody>
			</table>
{% endblock %}

