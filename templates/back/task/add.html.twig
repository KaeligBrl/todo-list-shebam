{% extends 'back/index.html.twig' %}

{% block title %}Tâche P1 - Ajouter - {% block title_base %}{{ parent() }}{% endblock %}{% endblock %}

{% block body %}
	<div>
{% block titre_admin_page %}{% block title_titre_admin_page_base %}{{ parent() }}{% endblock %}Tâche - P1{% endblock %}
	</div>
	{% block admin_utilisateur %}
		<div class="was-validated form-back background-login is-valid">
		{% if notification %}
			<div class="alert alert-success">{{ notification }}<br>
				<hr>
				<a href="{{ path('task_list_admin') }}">Retourner à la liste</a>
			</div>
		{% endif %}
			{{ form_start(form_task_add_admin) }}
			<div class="form-group">
				{{ form_row(form_task_add_admin.customer) }}
				{{ form_row(form_task_add_admin.object) }}
				{{ form_row(form_task_add_admin.subobject1) }}
				{{ form_row(form_task_add_admin.subobject2) }}
				{{ form_row(form_task_add_admin.subobject3) }}
				{{ form_row(form_task_add_admin.users) }}
				{{ form_row(form_task_add_admin.p1) }}
				{{ form_row(form_task_add_admin.p2) }}
				{{ form_row(form_task_add_admin.submit) }}
			</div>
		</div>
				{{ form_end(form_task_add_admin) }}
	{% endblock %}
{% endblock %}


