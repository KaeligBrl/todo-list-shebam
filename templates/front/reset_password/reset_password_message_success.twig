{% extends 'base_login_register.html.twig' %}

{% block title %}Mot de passe changé - {% block title_base %}{{ parent() }}{% endblock %}{% endblock %}

{% block content %}
<meta http-equiv="refresh" content="6;url={{ path('home') }}"/>

<a href="{{ path('home') }}">
    <img class="d-block mx-auto mb-4 auth-logo" src="{{ asset('/assets/images/logo-todo.png') }}" alt="Logo de connexion To Do List">
</a>

<div class="container-custom text-center">
    <div class="form-general auth-success-card">
        <div class="auth-success-badge">✓</div>

        <h1 class="mb-2 auth-page-title">Mot de passe changé</h1>
        <div class="separator mb-4"></div>

        <p class="mb-2 auth-text-white">Ton mot de passe a bien ete modifie.</p>
        <p class="mb-4 auth-text-soft">Redirection automatique vers la connexion dans 6 secondes.</p>

        <a href="{{ path('home') }}" class="btn btn-submit w-100 auth-submit-btn">Aller a la connexion maintenant</a>
    </div>
</div>

{% endblock %}